import './styles.css'
import DomManager from './modules/dom-manager'

document.addEventListener('DOMContentLoaded', () => {
  //const ui = new UI();
  //ui.load();
  //new UI().load();
  DomManager.init();
})
